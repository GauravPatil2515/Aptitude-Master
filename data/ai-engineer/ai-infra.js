/**
 * data/ai-engineer/ai-infra.js — Phase 7: AI Infrastructure
 */
export default {
  id: 'ai-engineer-ai-infra',
  subject: 'ai-engineer',
  title: 'Phase 7: AI Infrastructure',
  
  difficulty: 'medium',
  estimatedTime: 80,
  prerequisites: ['local-models'],

  notes: `
## Phase 7: AI Infrastructure

Running AI products requires a robust traditional software stack to handle API endpoints, async tasks, state persistence, caching, and containerized deployments.

---

### 1. Web API Layer: FastAPI

**FastAPI** is the primary choice for Python-based AI development due to:
*   **Asynchronous Support:** Built on ASGI, permitting native \\\`async/await\\\` to handle I/O bound LLM API calls without blocking.
*   **Pydantic Validation:** Automatically validates request/response schemas.
*   **Streaming Responses:** Supports Streaming HTTP and WebSockets for real-time streaming of generated characters.

---

### 2. State & Caching: PostgreSQL & Redis

*   **PostgreSQL:** Used to store persistent application states (user accounts, session history, metadata indexes).
    *   *JSONB Support:* PostgreSQL stores arbitrary nested payloads (like LangGraph states or tool outputs) in JSONB columns, enabling fast indexing on key paths.
*   **Redis:** In-memory key-value cache. Used for:
    *   *Session Caching:* Storing the active agent conversations.
    *   *Rate Limiting:* Restricting user API requests.
    *   *Message Broker:* Powering Celery task queues.

---

### 3. Asynchronous Processing: Celery

LLM operations or database extractions (like vectorizing 1,000 PDFs) can take minutes.
*   **Celery:** A distributed task queue that processes heavy workloads asynchronously outside the main FastAPI web server loop, preventing HTTP timeouts.

---

### 4. Deployment: Docker & Nginx

*   **Docker:** Containerizes applications to ensure consistency across local and production machines. Exposes ports and volumes for vector stores, database engines, and web apps.
*   **Nginx:** A reverse proxy and load balancer. It manages SSL certificates, terminates external connections, and routes requests to the internal FastAPI containers.

---

### 5. Build: Docker Compose File for AI Stack
A standardized deployment script using Docker Compose to orchestrate FastAPI, Redis, and Qdrant:

\\\`\\\`\\\`yaml
version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - REDIS_URL=redis://redis:6379/0
      - QDRANT_URL=http://qdrant:6333
    depends_on:
      - redis
      - qdrant

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
    volumes:
      - qdrant_data:/qdrant/storage

volumes:
  qdrant_data:
\\\`\\\`\\\`
  `,

  formulas: [
    { name: 'API Latency Equation', formula: 'Latency_Total = Latency_DNS + Latency_Proxy + Latency_LLM_Generation', example: 'Averaging 2.5 seconds on cloud configurations' },
    { name: 'Docker Port Map Syntax', formula: 'Host_Port:Container_Port', example: '8000:8000 exposes internal port 8000' }
  ],

  shortcuts: [
    'Always execute I/O operations (like fetching web pages or calling LLM APIs) inside async endpoints in FastAPI.',
    'Use Docker Compose to spin up local instances of Redis, Qdrant, and PostgreSQL during development.',
    'Offload heavy operations (e.g., fine-tuning, PDF indexing, batch embedding) to Celery background tasks.'
  ],

  questions: [
    {
      id: 'inf-q1',
      text: 'Why is FastAPI highly preferred over older frameworks like Flask for building LLM applications?',
      options: [
        'FastAPI executes Python code on GPUs automatically',
        'FastAPI has native asynchronous (async/await) support, preventing blocking of threads during slow I/O bound LLM API requests',
        'FastAPI has a built-in vector database engine',
        'FastAPI does not require any Python imports'
      ],
      answer: 1,
      explanation: 'LLM responses take seconds. FastAPI\'s async structure lets a single server handle thousands of concurrent requests by switching tasks during I/O waits.',
      hint: 'Think about async endpoints and thread blocking during long API calls.',
      difficulty: 'easy',
      tags: ['fastapi', 'async'],
      timeLimit: 45
    },
    {
      id: 'inf-q2',
      text: 'Which database column type in PostgreSQL is best suited for storing dynamic, unstructured JSON outputs generated by LLM agent tool calls?',
      options: ['VARCHAR(255)', 'BLOB', 'JSONB', 'TEXT'],
      answer: 2,
      explanation: 'JSONB stores JSON data in a decomposed binary format, allowing fast lookups, query filtering on nested properties, and native indexing.',
      hint: 'It stands for Binary JSON.',
      difficulty: 'medium',
      tags: ['database', 'postgres'],
      timeLimit: 60
    },
    {
      id: 'inf-q3',
      text: 'What is the role of Celery in an AI application?',
      options: [
        'To serve as the primary frontend framework',
        'To coordinate asynchronous background task processing for long-running jobs like document indexing and PDF vectorization',
        'To encrypt API endpoints',
        'To execute Cypher queries in Neo4j'
      ],
      answer: 1,
      explanation: 'Tasks like vectorizing large documents block web servers. Celery offloads these to background workers, returning immediately with a task ID.',
      hint: 'It is a distributed background task queue.',
      difficulty: 'medium',
      tags: ['celery', 'background-tasks'],
      timeLimit: 60
    },
    {
      id: 'inf-q4',
      text: 'In Docker Compose, what does the "depends_on" directive guarantee?',
      options: [
        'It compiles the dependent Docker image first',
        'It defines the order of startup for container services',
        'It automatically updates host port mappings',
        'It guarantees the dependent service is healthy before starting'
      ],
      answer: 1,
      explanation: 'depends_on configures the order in which services are started. For example, the web app service will start only after redis and qdrant are launched.',
      hint: 'It specifies order dependencies for container startup.',
      difficulty: 'medium',
      tags: ['docker', 'compose'],
      timeLimit: 60
    },
    {
      id: 'inf-q5',
      text: 'Which server acts as a reverse proxy, routes traffic to internal docker containers, and terminates SSL connections?',
      options: ['FastAPI', 'Redis', 'Nginx', 'Celery'],
      answer: 2,
      explanation: 'Nginx acts as a high-performance web server, reverse proxy, and load balancer, routing incoming external traffic to the correct backend containers.',
      hint: 'It is a widely used web server and reverse proxy.',
      difficulty: 'easy',
      tags: ['nginx', 'routing'],
      timeLimit: 45
    }
  ],

  aiTutorPrompt: 'You are an AI DevOps Engineer tutoring a student on Phase 7: AI Infrastructure. Explain async FastAPI setups, JSONB in PostgreSQL, Redis caching, Celery task distribution, Docker container orchestration, and Nginx reverse proxy setups.'
};
